{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/analyze-budget", "destination": "/api/analyze-budget" }
  ],
  "crons": [
    {
      "path": "/api/weekly-digest",
      "schedule": "0 8 * * 1"
    }
  ]
}
